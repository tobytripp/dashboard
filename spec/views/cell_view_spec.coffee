describe "CellView", ->
  cell = undefined

  it "is as wide as the widest of its two elements", ->

  describe "#flip", ->
    it "rotates the front/back classes on its containers", ->

  describe "#edit()", ->
    it "displays its edit form", ->
